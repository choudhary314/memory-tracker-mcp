[project]
name = "memory-tracker-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14.2"
dependencies = [
    "mcp[cli]>=1.25.0",
    "openai>=2.15.0",
]
